if [[ $OSTYPE =~ ^linux-gnu ]]; then
  eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"
elif [[ $OSTYPE =~ ^darwin ]]; then
  eval "$(/opt/homebrew/bin/brew shellenv)"
fi

# Add bin
export BIN=$HOME/bin
export DOTBIN=$HOME/.bin

# Add to $PATH
export PATH=$BIN:$DOTBIN:$PATH
